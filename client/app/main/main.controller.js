'use strict';

angular.module('donaloTdpApp')
  .controller('MainCtrl', function ($scope, $http, Campaign) {
    // Campaign.randomCampaign({campaignType:1}, function(campaign) {
    //   $scope.campaign = campaign;
    // });
  });
